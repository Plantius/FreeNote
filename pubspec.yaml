name: free_note
description: "A Note Taking and Calendar App."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  go_router: ^16.3.0
  provider: ^6.1.5+1
  flutter_dotenv: ^6.0.0
  logger: ^2.6.2
  flutter_markdown_plus: ^1.0.5
  url_launcher: ^6.3.2
  path_provider: ^2.1.5
  flutter_svg: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  assets: 
    - .env  

  uses-material-design: true